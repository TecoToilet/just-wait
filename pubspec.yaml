name: just_wait
description: A fisherman's patience for your attention span.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  device_apps: ^2.2.0
  flutter_foreground_task: ^6.1.2
  permission_handler: ^11.1.0
  installed_apps: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
