<!-- 
  android/app/src/main/AndroidManifest.xml
  
  IMPORTANT: This is NOT a complete manifest. 
  Add these sections to your Flutter-generated manifest.
  The Flutter template generates the base manifest when you run:
  flutter create just_wait
  
  Add these inside <application> tag:
-->

<!-- Accessibility Service declaration -->
<!--
<service
    android:name=".JustWaitAccessibilityService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="true">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>
-->

<!-- 
  Also add android:launchMode="singleTop" to the MainActivity activity tag
  so the intercept intent updates the existing activity rather than creating a new one:
-->
<!--
<activity
    android:name=".MainActivity"
    android:launchMode="singleTop"
    ...rest of existing attributes...>
-->

<!-- 
  Create this file: android/app/src/main/res/xml/accessibility_service_config.xml
  Contents below:
-->
<!--
<?xml version="1.0" encoding="utf-8"?>
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true"
    android:settingsActivity="com.justwait.app.MainActivity" />
-->

<!--
  Add to android/app/src/main/res/values/strings.xml:
  <string name="accessibility_service_description">
    Just Wait monitors app launches to activate attention gates for guarded apps.
  </string>
-->
